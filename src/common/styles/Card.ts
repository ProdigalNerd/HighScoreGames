import { StyleSheet } from "react-native";

const cardStyles = StyleSheet.create({
  container: {
    width: '100%',
    marginTop: '1rem',
    marginBottom: '1rem',
  }
});

export default cardStyles;
